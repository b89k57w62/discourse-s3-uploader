en:
  site_settings:
    s3_uploader_enabled: "Enable the S3 Uploader plugin for admin file uploads. Uses Discourse's built-in S3 configuration."
    s3_uploader_bucket_name: "Override S3 bucket name for uploads (leave empty to use Discourse's default S3 bucket)"